spring.datasource.url=jdbc:postgresql://172.25.47.154:5432/thesis
spring.datasource.username=nhanhoa
spring.datasource.password=123
spring.datasource.driver-class-name=org.postgresql.Driver

#configure Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.datasource.hikari.connection-init-sql=SET TIME ZONE 'Asia/Ho_Chi_Minh'

# File Upload - Production (use environment variable for flexibility)
# Set UPLOAD_DIR environment variable in production, fallback to /var/app/uploads
file.upload-dir=${UPLOAD_DIR:/var/app/uploads}